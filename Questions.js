// Question 1
function largestSwap(num) {
var str = 'num';
if ('num' > reverseString('num') {
    'num' = true;
}
return largestSwap
}

// Question 2
function isSpecialArray(arr) {
i = arr;
if (i % 1, i % 0); {
    return (true)}
    else (i % 0, i % 0 || i % 1, i % 1); {
        return (fasle)
    }
}
}

//Question 3
function formatNum(num) {
    return(num.toLocalString());
}